# Neon Postgres connection string (from your Neon project dashboard).
DATABASE_URL="postgresql://user:password@ep-xxxx.neon.tech/content_engine?sslmode=require"

# Anthropic API key — used server-side to research, ideate, and write copy.
ANTHROPIC_API_KEY=

# Optional: override the model used for generation (defaults to claude-opus-5).
ANTHROPIC_MODEL=
