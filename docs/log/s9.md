# S9 — Link pass

Branch `phase/s9-link-pass`. Opus 5.5, low effort (§1.37).

## Built
- ESLint 9 flat config (`eslint.config.mjs`: `next/core-web-vitals` + typescript-eslint recommended
  + `eslint-config-prettier`) and Prettier (`.prettierrc.json`: 2 spaces, double quotes, trailing
  commas, 100 cols). One autofix commit; one hand fix (unused type import in `research-ui.test.ts`).
- `npm run lint` (`eslint . && prettier --check .`) and `npm run format`; lint joins `verify` and CI.
- Header nav, final order: Content, Research ▾ (Outliers, Report, Questions, Compare), Studio ▾
  (Scripts, Plan, From listing), Facts, Lessons, YouTube ▾ (Digest, Topics, Marks, Sources, Ingest),
  Inbox. Groups open on hover/focus (tap on a phone); strings in the new `dict/header.ts`.
- `/brand/[id]` reads ideas server-side via the bridge and uses S6's `IdeaStatusTabs`, `IdeaActions`
  and `saveIdeaEdits`; `BrandIdeaCard`'s own approve/reject/copy buttons and the client fetch are gone.
- `/youtube/video/[id]`: `SaveLessonButton` under each key point (takeaway); `FallbackAnalyzeButton`
  when the video has no transcript (owner only).
- `KNOWN-ISSUES.md` rewritten: build-1 UNVERIFIED entries removed, only open cross-phase items.
- `docs/LOCAL-SETUP.md` gains the poll exit codes and the weekly-report `schtasks` line (o6, b2b-a);
  README links `docs/SUBSCRIPTION-MODE.md`; `media/` added to `.gitignore` (s12).

## Decisions
- Markdown is in `.prettierignore`: prompts, plan and logs are hand-wrapped prose, and reflowing
  them would churn every doc for no reader benefit. No ESLint rule disabled.
- `_`-prefixed names are allowed as unused (`no-unused-vars` options), matching the repo's idiom.
- `nav.ts` is pinned by O6's key snapshot test, so new nav strings live in `dict/header.ts`.

## Known issues
- No active-link highlight in the header yet (would need a client component for the pathname).
- Watcher Routine `content-engine lane 2 watcher` (`trig_01JhnXHVNNMNubyxqXc2Kqcj`) NOT deleted: the
  session's permission classifier refused `delete_trigger`. Anton deletes it in claude.ai → Routines.

Verification: CI green (`verify` incl. lint + `screenshots`) on 7fb0cb6; PR #42 merged as b6fc146 (tree
identical to 7fb0cb6, base unchanged). Local `npm run verify`: 297 unit, 156 integration, build.
