/** Owned by S10 (competitor research). Empty until then (PLAN.md §1.26) — the import in dictionary.ts is already wired (O7). */

export const en = {} as const;

export const sv: Record<keyof typeof en, string> = {};
